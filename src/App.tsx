import Resumes from "./components/resumes"; // Adjust the path if needed
import "./styles/style.css";              // Optional: import global CSS

function App() {
  return (
    <div>
      <Resumes />
    </div>
  );
}

export default App;
